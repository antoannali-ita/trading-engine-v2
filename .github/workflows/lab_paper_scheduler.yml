name: Strategy Lab Daily Opportunity Feed

on:
  workflow_dispatch:
  schedule:
    - cron: "30 14 * * 1-5"
    - cron: "30 17 * * 1-5"
    - cron: "30 20 * * 1-5"

permissions:
  contents: read

concurrency:
  group: strategy-lab-daily-opportunity-feed
  cancel-in-progress: false

jobs:
  paper:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      PYTHONPATH: ${{ github.workspace }}/laboratory/src
    steps:
      - name: Checkout strategy lab branch
        uses: actions/checkout@v4
        with:
          ref: strategy-lab-v1

      - name: Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: laboratory/requirements.txt

      - name: Install
        working-directory: laboratory
        run: pip install -r requirements.txt

      - name: Laboratory heartbeat start
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: python -m system_health.run_log start --module LAB_PAPER --component daily-opportunity-feed

      - name: Unit tests
        working-directory: laboratory
        run: pytest -q tests

      - name: Dynamic Exit heartbeat start
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: python -m system_health.run_log start --module DYNAMIC_EXIT --component recalibration

      - name: Recalibrate dynamic exits for active paper positions
        working-directory: laboratory
        env:
          LAB_DYNAMIC_EXIT_ENABLED: "1"
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: python jobs/run_dynamic_exit.py

      - name: Dynamic Exit heartbeat finish
        if: always()
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: python -m system_health.run_log finish --module DYNAMIC_EXIT --component recalibration --status "${{ job.status }}"

      - name: Generate opportunity ladder and paper feed
        working-directory: laboratory
        env:
          LAB_SYMBOLS: "SPY,QQQ,AAPL,MSFT,NVDA,AMZN,GOOGL,META,AVGO,AMD,JPM,GS,AXP,PGR,V,MA,UNH,LLY,NVO,CVS,COST,WMT,HD,CAT,GE,RTX,XOM,CVX,ADBE,CRM,ORCL,NFLX,FTNT,MUFG,TSM,ASML,BKNG,UBER,PANW,LIN"
          LAB_WATCH_SCORE: "50"
          LAB_MAX_POSITION_USD: "5000"
          LAB_MAX_NEW_BUYS: "12"
          LAB_MAX_ACTIVE_POSITIONS: "80"
          LAB_MAX_ACTIVE_PER_STRATEGY: "24"
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: python jobs/run_paper_signals.py

      - name: Verify stale watchlist cleanup
        working-directory: laboratory
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: python jobs/cleanup_watchlist_stale.py

      - name: Enrich V2 metadata, risk identity and Fineco cost scenarios
        working-directory: laboratory
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: python jobs/enrich_lab_metadata.py

      - name: Sync final signal state into active watchlist
        working-directory: laboratory
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: python jobs/sync_watchlist_final_state.py

      - name: Update signal outcomes D1-D60 MFE MAE
        working-directory: laboratory
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: python jobs/run_signal_outcomes.py

      - name: Sync fill-based execution and normalized R metrics
        working-directory: laboratory
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: python jobs/sync_paper_execution.py

      - name: Build atomic Laboratory 2.2 dashboard snapshots
        working-directory: laboratory
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
          GITHUB_RUN_ID: ${{ github.run_id }}
        run: python jobs/build_strategy_snapshots.py

      - name: Laboratory heartbeat finish
        if: always()
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: python -m system_health.run_log finish --module LAB_PAPER --component daily-opportunity-feed --status "${{ job.status }}"
